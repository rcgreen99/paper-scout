[project]
name = "paper-scout"
version = "0.1.0"
description = "LoRA training for arxiv recommender"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=3.5.1",
    "kaggle>=1.7.4.2",
    "peft>=0.15.2",
    "transformers>=4.51.3",
]
